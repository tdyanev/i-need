<?php

class LanguageTest extends TestCase {

    /*
    public function test_default_lang() {
        $output = $this->request('GET', 'register');

        $this->assertContains(
            'Username',
            $output,
            'Username label was found'
        );
        $this->assertContains(
            'Submit',
            $output,
            'Submit button was found'
        );
    }

    public function test_change_lang() {
        $this->request('GET', 'language/set/bulgarian');
        $this->assertResponseCode(302);

        $output = $this->request('GET', 'register');

        $this->assertContains(
            'Потребителско име',
            $output,
            'Username label was found'
        );
    }
*/
}
